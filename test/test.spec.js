'use strict';

var generateSteps = require('../lib');

describe('generateSteps', function() {
  it('should be a function', function() {
    expect(generateSteps).to.be.a('function');
  });

  it('should write some tests when I learn how to test a stream');
});
